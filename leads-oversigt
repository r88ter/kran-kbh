<!DOCTYPE html>
<html lang="da">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Lead Oversigt - Kranudlejning København</title>
  <meta name="description" content="Oversigt over varme leads til kranudlejning i Københavnsområdet. Hurtig adgang til nye kundehenvendelser." />
  <style>
    /* Reset */
    * {
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen,
        Ubuntu, Cantarell, "Open Sans", "Helvetica Neue", sans-serif;
      margin: 0;
      background: #f9f9f9;
      color: #222;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 1rem;
    }

    h1 {
      margin-bottom: 1rem;
      color: #0a74da;
      text-align: center;
    }

    #lead-list {
      list-style: none;
      padding: 0;
      max-width: 600px;
      width: 100%;
    }

    #lead-list li {
      background: white;
      margin-bottom: 1rem;
      padding: 1rem 1.5rem;
      border-radius: 6px;
      box-shadow: 0 2px 6px rgb(0 0 0 / 0.1);
      transition: box-shadow 0.2s ease;
      cursor: pointer;
    }

    #lead-list li:hover {
      box-shadow: 0 4px 12px rgb(0 0 0 / 0.15);
    }

    .lead-name {
      font-weight: 600;
      font-size: 1.1rem;
      margin-bottom: 0.2rem;
      color: #222;
    }

    .lead-email {
      font-size: 0.9rem;
      color: #555;
      margin-bottom: 0.5rem;
    }

    .lead-phone {
      font-size: 0.9rem;
      color: #555;
    }

    .no-leads {
      max-width: 600px;
      background: #fff3cd;
      color: #856404;
      border: 1px solid #ffeeba;
      padding: 1rem 1.5rem;
      border-radius: 6px;
      text-align: center;
      margin-top: 2rem;
      font-weight: 600;
    }

    @media (max-width: 480px) {
      #lead-list li {
        padding: 1rem;
      }
    }
  </style>
</head>
<body>
  <h1>Lead Oversigt - Kranudlejning København</h1>
  <ul id="lead-list">
    <li>Indlæser leads...</li>
  </ul>

  <script>
    // Skift denne URL til dit API endpoint, der returnerer leads som JSON
    const apiUrl = "https://dit-api-endpoint/lead";

    async function hentLeads() {
      const ul = document.getElementById("lead-list");
      ul.innerHTML = "<li>Indlæser leads...</li>";

      try {
        const res = await fetch(apiUrl);
        if (!res.ok) throw new Error("Netværksfejl");

        const data = await res.json();

        if (data.length === 0) {
          ul.innerHTML = '<div class="no-leads">Ingen nye leads lige nu.</div>';
          return;
        }

        ul.innerHTML = "";

        data.forEach((lead) => {
          const li = document.createElement("li");

          li.innerHTML = `
            <div class="lead-name">${lead.name || "Ukendt navn"}</div>
            <div class="lead-email">📧 ${lead.email || "-"}</div>
            <div class="lead-phone">📞 ${lead.phone || "-"}</div>
          `;

          // Tilføj eventuelt link eller funktionalitet til at se mere eller betale her
          li.addEventListener("click", () => {
            alert(`Se detaljer for ${lead.name || "lead"}`);
            // Eller redirect til betalingsside etc.
          });

          ul.appendChild(li);
        });
      } catch (error) {
        ul.innerHTML = `<div class="no-leads">Fejl ved hentning af leads. Prøv igen senere.</div>`;
        console.error(error);
      }
    }

    hentLeads();
  </script>
</body>
</html>

